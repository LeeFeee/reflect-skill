---
name: reflect-skill
description: Agent 自我复盘与进化技能。每日分析 session 日志，通过累积升级机制（观察→注意→待办→紧急）筛选出反复出现的问题，生成复盘报告，经用户逐条审批后更新 MEMORY.md、USER.md、SOUL.md、AGENTS.md。所有修改可溯源，支持备份恢复。当用户说"复盘"、"reflect"、"审核"、"审批"时触发，也可通过 agent:bootstrap 事件自动触发。
metadata:
  openclaw:
    events:
      - agent:bootstrap
---

# Reflect Skill — Agent 自我复盘与进化

## 执行要求

- **阶段 A（自动分析）**：只读 session 日志 + 写入 reports/ 目录下的报告文件。**禁止修改任何核心配置文件**（MEMORY.md、USER.md、SOUL.md、AGENTS.md）。
- **阶段 B（审批与修改）**：修改核心文件前**必须先备份**到 backups/ 目录。所有核心文件修改**必须经用户逐条批准**，未获批准的条目不得写入。
- **禁止删除**：不主动删除核心配置中的现有内容，只做追加或替换。
- **一份文件三个角色**：reports/ 下的报告文件同时承担「每日日报」「待办清单」「修改记录（Changelog）」三个角色。

## 文件结构

```
{skill 目录}/
├── SKILL.md              ← 当前文件
├── _meta.json
├── reports/              ← 每日报告（= 待办清单 = changelog）
│   └── YYYY-MM-DD.md
└── backups/              ← 核心文件原始备份
    └── {文件名}.{YYYYMMDD}
```

所有产出物（报告、备份）均在 skill 目录内。

## 两阶段执行模型

### 阶段 A：自动分析（Step 1-4）

每次会话启动时（通过 `agent:bootstrap` 事件），检测是否需要生成当日报告。阶段 A 只做只读操作 + 写入报告文件，**不修改任何核心配置文件**。

### 阶段 B：审批与应用（Step 5-7）

检测到 reports/ 下有待审批的报告时，进入交互模式。展示待办清单，用户逐条审批后执行修改、归档。

---

## 累积升级机制

### 严重等级

| 等级 | 英文 | 含义 | 何时产生 | 是否进入审批区 |
|------|------|------|----------|---------------|
| 观察 | observe | 首次发现，仅记录 | 首次出现 | 否 |
| 注意 | watch | 再次出现，持续关注 | 第 2 次出现 | 否 |
| 待办 | todo | 反复出现，建议升级 | 第 3 次出现 | 是 |
| 紧急 | urgent | 严重问题，立即升级 | 数据丢失/安全/功能阻断 | 是 |

### 累积匹配逻辑

1. 为每个发现提取 **pattern-key**（简短、稳定的标识，同类问题使用相同 key，如 `reply_style_concise`、`lark_im_timeout`）。
2. 读取 reports/ 下所有历史报告，搜索相同 pattern-key，累加出现次数。
3. 根据累积计数判定等级：
   - 第 1 次 → observe
   - 第 2 次 → watch
   - 第 3 次 → todo（进入审批区）
   - 数据丢失 / 安全问题 / 功能阻断 → urgent（直接进入审批区，不依赖计数）

### 累积示例

```
04-06 报告：用户不喜欢长回复 (key: reply_style_concise, count=1, observe)
04-07 报告：用户再次抱怨回复太长 (key: reply_style_concise, count=2, watch)
04-08 报告：用户第三次提到     (key: reply_style_concise, count=3, todo → 进入审批)
```

### 用户干预

用户在审批时可以主动将「观察记录」区的 observe/watch 级别条目提前升级为待办，纳入审批流程。

---

## 报告文件规范

### 报告路径

`reports/YYYY-MM-DD.md`（skill 目录内）。

### 报告模板

```markdown
---
date: YYYY-MM-DD
status: pending | completed
---

# YYYY-MM-DD 复盘报告

## 昨日概要
- 做了什么
- 做对的事
- 做错的事

## 观察记录（仅记录，不修改）

- 🔍 [pattern-key] 描述 (count: N, 首次: YYYY-MM-DD)

## 待办清单（用户审批区）

### N. [待审批|已批准|已拒绝] {目标文件} — {简短标题}
**等级**：待办（累积 N 次，首次 YYYY-MM-DD）或 紧急
**操作**：追加 | 替换
**内容**：具体要写入的文本
**理由**：为什么需要修改

## 修改记录（Changelog）

| # | 文件 | 操作 | 状态 | 等级 | 时间 |
|---|------|------|------|------|------|
```

### 状态流转

- `pending`：有待审批的待办项，等待阶段 B 处理
- `completed`：所有待办项已处理（批准或拒绝）

### 三合一说明

- **生成时**：报告是「每日日报」+「待办清单」
- **审批后**：待办清单变为「修改记录（Changelog）」，每条标记最终状态
- **无待办项时**：status 直接设为 completed，跳过阶段 B

---

## 七步 SOP

### Step 1: 确定时间与范围（阶段 A）

1. 确定复盘的 Agent ID（默认 `main`）。
2. 确定目标日期（默认昨天），格式 `YYYY-MM-DD`。如果用户指定了日期则使用用户指定的日期。
3. 检查 `reports/YYYY-MM-DD.md` 是否已存在：
   - 不存在 → 继续下一步
   - 存在且 `status: pending` → 跳转 Step 5（阶段 B）
   - 存在且 `status: completed` → 终止，告知用户该日复盘已完成

### Step 2: 收集数据（阶段 A）

1. 进入 `~/.openclaw/agents/${agentId}/sessions` 目录。
2. 读取所有带有目标日期时间戳的 `.jsonl` 会话记录。
3. 提取所有 user 指令、assistant 回复、工具调用及工具结果。

### Step 3: 分析与累积匹配（阶段 A）

1. 按 6 个维度分析 session 内容：
   - **昨日动作**：做了哪些事情
   - **做对的事**：一次完成、获得用户称赞的事项
   - **做错的事**：需要重复修正、被用户批评的事项
   - **改进点**：针对做错的事，提出可执行的防范措施
   - **用户画像**：偏好、风格、底线
   - **Agent 画像**：行为倾向、暴露的缺陷
2. 为每条发现生成 pattern-key（简短、稳定、同类问题使用相同 key）。
3. 读取 `reports/` 下所有历史报告，搜索相同 pattern-key，累加出现次数。
4. 根据累积计数判定等级：
   - 第 1 次 → observe
   - 第 2 次 → watch
   - 第 3 次 → todo
   - 数据丢失/安全/功能阻断 → urgent（不依赖计数）

### Step 4: 生成报告（阶段 A）

1. 按照「报告文件规范」章节的模板，写入 `reports/YYYY-MM-DD.md`。
2. observe 和 watch 级别条目 → 写入「观察记录」区。
3. todo 和 urgent 级别条目 → 写入「待办清单」区，每条需包含：
   - 目标文件（MEMORY.md / USER.md / SOUL.md / AGENTS.md）
   - 操作类型（追加 / 替换）
   - 具体要写入的文本内容
   - 修改理由
4. 判断报告状态：
   - 有待办清单项 → `status: pending`
   - 无待办清单项 → `status: completed`
5. 向用户简要报告：今日复盘完成，N 条观察记录，M 条待审批项。
   - 如果 `status: completed`，流程到此结束。
   - 如果 `status: pending`，告知用户有待审批项等待处理。

### Step 5: 展示与审批（阶段 B）

1. 读取 `reports/` 下 `status: pending` 的最新报告。
2. 向用户展示待办清单摘要（每条显示：目标文件、操作类型、内容摘要）。
3. 等待用户逐条决定：
   - **批准**：标记为「已批准」，进入 Step 6 执行
   - **拒绝**：标记为「已拒绝」，记录用户拒绝理由
   - **修改内容后批准**：根据用户反馈调整条目内容，再标记为「已批准」
4. 用户可要求将「观察记录」区的条目提前升级为待办，纳入审批流程。
5. 用户可全部批准、全部拒绝或部分批准。

### Step 6: 执行修改（阶段 B）

对每条已批准的待办项，**严格按顺序逐条执行**：

1. **备份**：将目标文件的当前版本完整复制到 `backups/` 目录。
   - 命名格式：`{文件名}.{YYYYMMDD}`
   - 同一天同一文件多次修改时追加序号：`{文件名}.{YYYYMMDD}.{N}`
2. **修改**：按操作类型执行。
   - **追加**：将新内容添加到目标文件的对应章节末尾
   - **替换**：用新内容替换目标文件中的指定旧内容
3. 更新报告中该条目的状态为「已批准」并记录时间戳。

### Step 7: 归档（阶段 B）

1. 将报告中所有待办项的最终状态汇总到「修改记录（Changelog）」表格中：
   - 每条记录包含：序号、文件、操作、状态（已批准/已拒绝）、等级、完成时间
2. 将报告 frontmatter 中的 `status` 更新为 `completed`。
3. 向用户发送归档确认：列出本次批准 N 条修改、拒绝 M 条，涉及的文件列表。

---

## 核心文件更新规则

### 目标文件与内容对应

| 目标文件 | 更新内容 | 来源 |
|----------|---------|------|
| MEMORY.md | 关键结论、重要事实 | 做对/做错 + 改进点 |
| USER.md | 用户画像、偏好、风格 | 用户画像维度 |
| SOUL.md | 行为准则、性格边界 | Agent 画像维度 |
| AGENTS.md | 工具约束、战术准则 | 改进点维度 |

### 更新方式

- **追加**：新增条目追加到对应章节末尾
- **替换**：对已有内容更新时，替换原文
- **禁止删除**：不主动删除核心配置中的现有内容

---

## 触发机制

### 自动触发（agent:bootstrap）

每次 OpenClaw 会话启动时，skill 自动激活，执行判断逻辑：

1. 检查 `reports/` 下今日报告是否存在
2. 不存在 → 执行阶段 A（Step 1-4）
3. 存在且 `status: pending` → 执行阶段 B（Step 5-7）
4. 存在且 `status: completed` → 静默退出，不打扰用户

### 手动触发

用户在对话中说"复盘"、"reflect"、"审核"、"审批"时也可手动触发。

手动触发时，默认对"昨天"进行复盘；用户可指定具体日期。
